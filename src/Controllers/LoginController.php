<?php 

class LoginController
{
     public function index()
     {
          // $login = "Vous êtes sur la page de connexion";
          
          // $loader = new \Twig\Loader\FilesystemLoader('../Views');
          // $twig = new \Twig\Environment($loader); // On instancie twig

          // echo $twig->render('login/login.view.php', compact('login'));
     }
}
